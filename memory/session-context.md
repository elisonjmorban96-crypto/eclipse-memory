# Session Context — Current Baseline

There is no pinned long-lived session narrative right now.

Use this order when building context:
1. Live runtime config first
2. `memory/2026-04-14-memory-correction.md` second
3. Project-specific source-of-truth files next
4. Older index/dashboard files only as historical hints

Current high-signal watch items:
- DevHouseAI / GHL workflow and template API access is still limited in the current setup.
- Morning briefing output can be stale.
- Upwork follow-up cron is healthy, but the active DB snapshot is empty.
