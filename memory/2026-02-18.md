# 2026-02-18 — System Infrastructure Overhaul

**Session type:** Deep infrastructure / maintenance
**Done by:** Claude Sonnet 4.6 (Claude Code session — not an openclaw agent session)
**Duration:** ~6 hours
**Result:** Gateway fully healthy, all patches applied, TTS working, cron policy enforced

---

## What Was Fixed

### 1. Cloud Code Assist API 400 Error — RESOLVED ✅
**Problem:** `sanitizeToolsForGoogle()` only ran for `google-gemini-cli`, not `google-antigravity`. Tool schemas with `patternProperties` passed through to Cloud Code Assist API and caused:
> `400: Unknown name "patternProperties" at request.tools[...].parameters.properties[...].value`

**Fix:** Added `google-antigravity` to the provider guard in ALL FOUR dist files that contain this function. Also fixed `cleanSchemaForGeminiWithDefs` to recurse into all object-valued schema keys (previously missed `not`, `if/then/else`, etc.).

**Files patched:**
- `/opt/homebrew/lib/node_modules/openclaw/dist/reply-oSe13ewW.js` ← CRITICAL (WhatsApp pipeline)
- `/opt/homebrew/lib/node_modules/openclaw/dist/subagent-registry-DN6TUJw4.js` ← CRITICAL
- `/opt/homebrew/lib/node_modules/openclaw/dist/pi-embedded-BxoxxVJz.js`
- `/opt/homebrew/lib/node_modules/openclaw/dist/pi-embedded-_crSbDV9.js`

**Patches archived:** `workspace/patches/openclaw/` (pushed to eclipse-dashboard)

---

### 2. Orphaned Tool Call Sanitizer — ACTIVE ✅
**Problem:** When sessions were interrupted mid-tool-call, `toolCall` blocks had no matching `toolResult`. Anthropic rejected these with:
> `tool_use ids found without tool_result blocks`

**Fix:** `sanitizeOrphanedToolCalls()` added to `LocalSessionHistory.readMessages()` in:
- `/Users/elisoninc./.openclaw/extensions/kimi-claw/dist/src/acp-gateway/local-session-history.js`

Strips orphaned tool calls at read time before they reach the LLM. All existing sessions had orphaned calls (16–336 each) — all handled.

---

### 3. ElevenLabs TTS — NOW WORKING ✅
**Problem:** TTS was disabled. Provider was `openai`. `auto` was `always` (every message got voice).

**Fixes applied:**
- `ELEVENLABS_API_KEY` added to `~/.openclaw/.env`
- `SAG` skill entry removed from `openclaw.json` (SAG = ElevenLabs, was duplicate)
- TTS enabled via gateway RPC: `tts.enable`
- Provider set to `elevenlabs` via `tts.setProvider`
- Auto set to `off` in `~/.openclaw/settings/tts.json` (briefings call TTS explicitly)

**Result:** Brian (morning) and George (evening) voice briefings work. Regular WhatsApp replies are text-only.

---

### 4. Cron Execution-Only Policy — ENFORCED ✅
**Rule:** Cron jobs must not invoke an LLM unless explicitly designated.

**New files:**
- `workspace/CRON-POLICY.md` — full policy doc
- `workspace/scripts/cron-exec-wrapper.js` — runs shell commands, writes JSON result, no LLM fallback
- `workspace/scripts/process-cron-results.js` — reads cron-results/, logs data-only, surfaces `interpretation_required=true`
- `workspace/scripts/process_triggers.js` — updated to call process-cron-results.js each heartbeat

**jobs.json changes:**
- **Follow-up Sweep** → `systemEvent` (was `agentTurn`) — runs `node runner.js --followup` via wrapper
- **Credit Monitor** → `systemEvent` (was `agentTurn`) — stays disabled
- **Morning Briefing, Evening Briefing, Nightly Wind-down** → unchanged `agentTurn` (explicitly LLM)

---

### 5. API Key Consolidation ✅
**Single source of truth for env-var secrets:** `~/.openclaw/.env`

| Secret | Location |
|--------|----------|
| `OPENAI_API_KEY` | `~/.openclaw/.env` |
| `ELEVENLABS_API_KEY` | `~/.openclaw/.env` |
| Notion / Vercel keys | `openclaw.json` → `skills.entries` |
| Kimi bridge token | `openclaw.json` → `plugins.entries.kimi-claw` |
| Anthropic / Google auth | `auth-profiles.json` (auto-managed by openclaw) |

---

### 6. eclipse-dashboard & eclipse-memory Repos — CLEAN ✅
- `eclipse-dashboard` (`elisonjmorban96-crypto/eclipse-dashboard`) = workspace repo
- `eclipse-memory` (`elisonjmorban96-crypto/eclipse-memory`) = model monitor deploy (Render)
- Render only auto-deploys from eclipse-memory (render.yaml removed from dashboard)
- Workspace `.gitignore` excludes node_modules, secrets, logs, tmp, *.db, *.mp3

---

### 7. eclipse-memory Model Monitor — HEALTHY ✅
- URL: `https://eclipse-memory.onrender.com/model-status`
- All 4 providers showing `available`: gemini, anthropic/haiku-4-5, anthropic/sonnet-4-5, openai/gpt-5.2
- Note: `/health` and `/status` don't exist — correct endpoint is `/model-status`

---

### 8. Gateway — CLEAN ✅
- Restarted fresh (2026-02-18)
- 16 old session JSONL files archived
- 12 stale delivery queue entries cleared (TTS temp files from Feb 14-15)
- Running on port 18789, WhatsApp linked
- kimi-claw ACP bridge connected to Kimi

---

## Key Decisions Made

### DEC-2026-02-18-PATCH-STRATEGY (locked)
All 4 dist files containing `sanitizeToolsForGoogle` must be patched on every openclaw update:
`reply-oSe13ewW.js`, `subagent-registry-DN6TUJw4.js`, `pi-embedded-BxoxxVJz.js`, `pi-embedded-_crSbDV9.js`
Patches stored in `workspace/patches/openclaw/README.md` with line numbers.

### DEC-2026-02-18-CRON-POLICY (locked)
Cron jobs are execution-only by default. LLM is only invoked for explicitly designated briefing jobs.

### DEC-2026-02-18-SECRETS (locked)
`~/.openclaw/.env` is the canonical store for env-var secrets. Do not store API keys in shell profiles or duplicate across files.

### DEC-2026-02-18-TTS (locked)
TTS auto=off always. Briefing jobs call the `tts` tool explicitly. Regular WhatsApp replies are text-only.

---

## What's Still Pending / Watch

- **Briefings failing with rate_limit:** Morning/Evening/Wind-down all have `consecutiveErrors > 0` due to all 3 providers (gemini-cli, antigravity, haiku) hitting rate limits simultaneously. Will self-clear as cooldowns expire.
- **openclaw dist patches:** Must be re-applied if openclaw updates (`npm update -g openclaw`). Check `patches/openclaw/README.md` for instructions.
- **Nightly Dev Build** (disabled): When re-enabled, scope needs better definition — currently too broad ("do your best work").

---

## Files Changed (summary)

```
~/.openclaw/.env                                          — ELEVENLABS_API_KEY added
~/.openclaw/settings/tts.json                             — auto: off, provider: elevenlabs
~/.openclaw/cron/jobs.json                                — Follow-up Sweep + Credit Monitor → systemEvent
~/.kimi/kimi-claw/openclaw.json                           — sag skill entry removed
~/.openclaw/extensions/kimi-claw/.../local-session-history.js — orphaned tool call sanitizer
/opt/homebrew/lib/node_modules/openclaw/dist/reply-oSe13ewW.js       — PATCHED
/opt/homebrew/lib/node_modules/openclaw/dist/subagent-registry-DN6TUJw4.js — PATCHED
/opt/homebrew/lib/node_modules/openclaw/dist/pi-embedded-BxoxxVJz.js — PATCHED
/opt/homebrew/lib/node_modules/openclaw/dist/pi-embedded-_crSbDV9.js — PATCHED
workspace/patches/openclaw/                               — patch archive (README + 3 files)
workspace/AUDIT.md                                        — system audit
workspace/CRON-POLICY.md                                  — cron execution-only policy
workspace/scripts/cron-exec-wrapper.js                    — NEW
workspace/scripts/process-cron-results.js                 — NEW
workspace/scripts/process_triggers.js                     — updated
```

---

## Session Extension — Kimi API Wiring (same day, later)

### What was done
- Wired `opencode/kimi-k2.5-free` as **fallback #3** in the model chain (free, 256k context)
- Also configured `kimi-coding/k2p5` (paid backup, not in fallback chain)
- Cleaned up `DECISIONS.md` — added 4 new 2026-02-18 decisions, corrected model routing entry
- Fixed `MEMORY.md` byte-split corruption (memory2.md truncated line, orphaned artifact on line 1)
- Fixed `memory_rotate.sh` — now splits at line boundaries (Python), not bytes
- Ran rotation: `memory3.md` created, `MEMORY.md` trimmed to 12,969 bytes

### DEC-2026-02-18-KIMI-ROUTING (locked)
- Fallback #3: `opencode/kimi-k2.5-free` — free via openclaw platform, 256k ctx, no key needed
- Paid backup: `kimi-coding/k2p5` — key in `~/.openclaw/.env` + LaunchAgent plist + `~/.kimi/kimi-claw/kimi-claw-config.json`
- `kimi-claw-config.json` must have `defaultModel.enabled: true` for paid provider to register

### Files changed (extension)
```
~/.openclaw/openclaw.json                                 — kimi-coding provider added to models.providers; fallback chain updated
~/.openclaw/.env                                          — OPENCLAW_DEFAULT_MODEL_API_KEY added
~/Library/LaunchAgents/ai.openclaw.gateway.plist         — OPENCLAW_DEFAULT_MODEL_API_KEY injected
~/.kimi/kimi-claw/kimi-claw-config.json                  — NEW; kimiCodeAPIKey + defaultModel config
~/.kimi/kimi-claw/openclaw.json                          — kimiCodeAPIKey added to bridge config
workspace/memory/MEMORY.md                               — artifact removed, stale decisions corrected, 2026-02-18 decisions added
workspace/memory/memory2.md                              — truncated final line completed
workspace/memory/memory3.md                              — NEW overflow archive (rotation)
workspace/memory_rotate.sh                               — byte-split bug fixed (Python line-boundary)
workspace/memory/DECISIONS.md                            — fully synced to current state
workspace/memory/RECALL-INDEX.md                         — Kimi routing + infra entries added
workspace/memory/STATUS-DASHBOARD.md                     — model routing updated to 4-provider chain
```
