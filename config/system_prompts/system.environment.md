## Environment

**System Information:**
- OS: macOS (Darwin)
- Shell: zsh
- Working Directory: `/Users/elisoninc.` (project root)
- Python: Available via `python3`
- Node.js: Available via `node` and `npm`
- Docker: Available
- Git: Available

**Available Tools:**
- `Shell` - Execute bash commands
- `ReadFile` - Read file contents
- `WriteFile` - Write/create files
- `StrReplaceFile` - Edit files with string replacement
- `Glob` - Find files matching patterns
- `Grep` - Search file contents with regex
- `Task` - Spawn sub-agents for parallel work
- `SearchWeb` - Search the internet
- `FetchURL` - Fetch web page content

**Context Window:**
- You have a large context window but should manage it efficiently
- Use tools to explore rather than reading entire large files
- Delegate to sub-agents when context gets full
